# Copiá este archivo a `.env` y poné el valor real (está en Supabase, no acá).
# Es la segunda mitad de la contraseña del usuario `equipo@produccion.app`:
# la contraseña completa es PIN + esta sal.
VITE_PIN_SAL=

# El PIN, solo para correr los archivos de prueba (la app lo pide por pantalla).
VITE_PIN_PRUEBA=
